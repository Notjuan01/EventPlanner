@page
@model EventPlanner.Pages.Recursos.IndexModel
@{
    ViewData["Title"] = "Index Recurso";
}

<div class="container mt-4">
    <h1 class="text-center text-primary">Recursos</h1>

    <div class="d-flex justify-content-between mb-3">
        <a asp-page="Create" class="btn btn-outline-primary btn-lg">
            <i class="bi bi-plus-circle"></i> Crear nuevo recurso
        </a>
    </div>

    <table class="table table-bordered table-striped">
        <thead class="thead-dark">
            <tr>
                <th scope="col">
                    @Html.DisplayNameFor(model => model.Recursos[0].IdEvento)
                </th>
                <th scope="col">
                    @Html.DisplayNameFor(model => model.Recursos[0].NombreRecurso)
                </th>
                <th scope="col">
                    @Html.DisplayNameFor(model => model.Recursos[0].Cantidad)
                </th>
                <th scope="col">
                    @Html.DisplayNameFor(model => model.Recursos[0].Decripcion)
                </th>
                <th scope="col">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Recursos)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.IdEvento)</td>
                    <td>@Html.DisplayFor(modelItem => item.NombreRecurso)</td>
                    <td>@Html.DisplayFor(modelItem => item.Cantidad)</td>
                    <td>@Html.DisplayFor(modelItem => item.Decripcion)</td>
                    <td>
                        <a asp-page="./Editar" asp-route-id="@item.Id" class="btn btn-warning btn-sm">
                            <i class="bi bi-pencil"></i> Editar
                        </a>
                        <a asp-page="./Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm">
                            <i class="bi bi-trash"></i> Eliminar
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>